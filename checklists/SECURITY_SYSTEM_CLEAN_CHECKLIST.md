# 🛡️ Чеклист безопасности

## «Система считается чистой»

Документ используется **после экстренной смены пароля офлайн** скрипт `emergency_passwd_offline.sh` и **перед возвращением системы в онлайн** скрипт `security_check_before_online.sh`.
Цель — принять **осознанное решение**, что система не имеет явных признаков компрометации.

## 🟥 Правило остановки

❗ Если **любой пункт имеет статус FAIL**, система **считается НЕ чистой**, сеть **НЕ следует включаеть** до выясненмя причин.

## 1️⃣ Сетевое состояние (OFFLINE)

**Статус:** ⬜ OK ⬜ WARN ⬜ FAIL

* [ ] Все сетевые интерфейсы отключены (`ip a`)
* [ ] Нет IP-адресов, кроме `lo`
* [ ] `ss -tulpn` не показывает ESTABLISHED-соединений

**Комментарий:**

## 2️⃣ Активные сетевые процессы

**Статус:** ⬜ OK ⬜ WARN ⬜ FAIL

* [ ] Нет неизвестных процессов, слушающих порты
* [ ] `lsof -i -P -n` показывает только ожидаемые сервисы

**Комментарий:**

## 3️⃣ Логи аутентификации

**Статус:** ⬜ OK ⬜ WARN ⬜ FAIL

* [ ] Нет повторяющихся `authentication failure`
* [ ] Нет попыток входа в нерабочее время
* [ ] Нет неизвестных пользователей

Файл: `/var/log/auth.log`

**Комментарий:**

## 4️⃣ SUDO-активность

**Статус:** ⬜ OK ⬜ WARN ⬜ FAIL

* [ ] Нет частых ошибок `sudo`
* [ ] Нет неожиданных вызовов sudo

**Комментарий:**

## 5️⃣ Запущенные процессы

**Статус:** ⬜ OK ⬜ WARN ⬜ FAIL

* [ ] Нет неизвестных бинарников
* [ ] Нет процессов из `/tmp`, `/dev/shm`
* [ ] Нет процессов без понятного назначения

**Комментарий:**

## 6️⃣ Автозапуск пользователя

**Статус:** ⬜ OK ⬜ WARN ⬜ FAIL

* [ ] `~/.config/autostart` пуст или понятен
* [ ] Нет новых `.desktop` файлов

**Комментарий:**

## 7️⃣ Systemd-сервисы

**Статус:** ⬜ OK ⬜ WARN ⬜ FAIL

* [ ] Нет неизвестных enabled-сервисов
* [ ] Недавние сервисы понятны и ожидаемы

Команда:

```bash
systemctl list-unit-files --state=enabled
```

**Комментарий:**

## 8️⃣ Git hooks (критично)

**Статус:** ⬜ OK ⬜ WARN ⬜ FAIL

* [ ] Нет custom hooks (субъективных) кроме сознательных
* [ ] Нет создаваемых hooks без необходимости

Команда:

```bash
find ~ -path "*/.git/hooks/*" -type f -not -name "*.sample"
```

**Комментарий:**

## 9️⃣ Планировщики задач

**Статус:** ⬜ OK ⬜ WARN ⬜ FAIL

* [ ] `crontab -l` пуст или ожидаем
* [ ] Нет чужих cron-заданий

**Комментарий:**

## 🔟 Переменные окружения

**Статус:** ⬜ OK ⬜ WARN ⬜ FAIL

* [ ] Нет подозрительных `LD_PRELOAD`
* [ ] Нет неизвестных PATH

Команда:

```bash
printenv
```

**Комментарий:**

## 1️⃣1️⃣ Домашний каталог пользователя

**Статус:** ⬜ OK ⬜ WARN ⬜ FAIL

* [ ] Нет недавно созданных исполняемых файлов
* [ ] Нет скрытых бинарников

**Комментарий:**

## 1️⃣2️⃣ Итоговое решение

Отметьте одно:

* ⬜ **СИСТЕМА СЧИТАЕТСЯ ЧИСТОЙ** → сеть можно включать
* ⬜ **ЕСТЬ СОМНЕНИЯ** → требуется дополнительный анализ
* ⬜ **СИСТЕМА НЕ ЧИСТА** → сеть НЕ включать

**Ответственный:** ______________________

**Дата:** ______________________

## 📌 Примечания

* Чеклист **не гарантирует 100% безопасности**
* Он подтверждает отсутствие **явных признаков компрометации**
* Используется как процедурный стандарт

🧭 Рекомендуется хранить файл рядом с `SECURITY.md` или в REBK как часть security-процедуры.

